<div>
    <x-table-header>
        <div class="flex items-center space-x-2">
            <x-icons.user class="w-4 h-4"/>
            <span>{{ __('Usuario') }}</span>
        </div>
    </x-table-header>

    <x-table-header>
        <div class="flex items-center space-x-2">
            <x-icons.description class="w-4 h-4"/>
            <span>{{ __('Acción') }}</span>
        </div>
    </x-table-header>

    <x-table-header>
        <div class="flex items-center space-x-2">
            <x-icons.table class="w-4 h-4"/>
            <span>{{ __('Modelo') }}</span>
        </div>
    </x-table-header>

    <x-table-header>
        <div class="flex items-center space-x-2">
            <x-icons.changes class="w-4 h-4"/>
            <span>{{ __('Cambios') }}</span>
        </div>
    </x-table-header>

    <x-table-header>
        <div class="flex items-center space-x-2">
            <x-icons.ip-address class="w-4 h-4"/>
            <span>{{ __('Ip Address') }}</span>
        </div>
    </x-table-header>

    <x-table-header>
        <div class="flex items-center space-x-2">
            <x-icons.timedate class="w-4 h-4"/>
            <span>{{ __('Fecha actualización') }}</span>
        </div>
    </x-table-header>
</div>
